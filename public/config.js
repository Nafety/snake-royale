// Configuration côté front (valeurs statiques synchronisées avec server/config.js)
// Ces valeurs sont chargées une seule fois au démarrage
export const clientConfig = {
  game: {
    pixelSize: 40
  }
};
